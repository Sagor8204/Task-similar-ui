export default function TabButton({ children }) {
  return (
    <button className="px-4 py-[6px] text-sm font-semibold rounded-md shadow-sm border border-gray-300">
      {children}
    </button>
  );
}
